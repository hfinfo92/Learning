--abrar alam -Snowflake data engineer
-- Create a table named "employees" with columns for employee ID, name, position, and hire date
CREATE OR REPLACE TABLE employees (
    employee_id INT,
    name STRING,
    position STRING,
    hire_date DATE
);  